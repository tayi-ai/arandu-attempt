# What this package declares about itself.
#
# The declaration is capability, not intent, and `aru doctor` compares it with
# what the code imports and calls. A package that says it makes no outbound
# calls and then holds an HTTP client is rejected, which is what makes the
# four lines below worth reading before installing anything.

name = "tayi-ai/attempt"
framework = ">= 0.46"
profiles = ["conventional", "performance"]

[permissions]
# It answers requests and reads its own rows. Nothing leaves the process.
network = false
# sandbox.go gives one attempt a writable tree of its own: it copies the task in,
# then reads and writes files under that root and nowhere else.
filesystem = true
# sandbox.go runs `go test` inside that tree to find out whether an attempt
# actually worked. This is the widest thing the package does, and it is the point
# of the package.
exec = true
# module.go owns the attempts table, so whoever installs this has to run
# `aru migrate`. Declaring it is how they find that out before deploying.
migrations = true
